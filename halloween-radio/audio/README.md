Halloween radio audio assets.

Uploaded tracks are stored in this folder for the Halloween brew radio project.

Filename convention: lowercase kebab-case only (for example: `coffin-lid-rag.mp3`). Use hyphens instead of spaces; avoid capitals, commas, parentheses, and upload suffixes such as `(1)`.
